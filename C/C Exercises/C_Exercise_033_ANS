#include <stdio.h>
  
int my_pow(int x, int y) {
    int sum = 1;
    for(int i=1; i<=y; i++)
    {
        sum = sum * x;
    }
    return sum;
}

int main(void) {
    int x, y;
    scanf("%d%d", &x, &y);
    printf("%d\n", my_pow(x, y) );

    return 0;
}
