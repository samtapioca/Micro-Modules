#include <stdio.h>

int multiTable(int num1, int num2) {
  if(num2 != 1)
    {
      multiTable(num1,num2-1);
    }
    printf("%d\n",(num1*num2));
    return 0;
}

int main() {
  multiTable(11,10);
  return 0;
}
