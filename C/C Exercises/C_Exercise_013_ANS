#include <stdio.h>
#include <math.h>

int main(void) {
    double volume;
    double r;
    double pi = 3.14159265;  // don't change the value of pi

    scanf("%lf", &r);
    volume = 4.0 / 3.0 * pi * pow(r, 3.0);   // change this line only

    printf("%f\n", volume);
    return 0;
}
