#include <stdio.h>

int main()
{
  int l;
  int b;

  printf("Enter length: ");
  scanf("%d", &l);
  printf("Enter base: ");
  scanf("%d", &b);

  printf("Area is %d\n",l*b);
  printf("Perimeter is %d\n",2*(l+b));
  
  return 0;
}
									
