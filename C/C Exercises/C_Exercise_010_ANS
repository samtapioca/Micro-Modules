#include <stdio.h>
#include <math.h>

int main(void) {
    double x, y;
    double distance;
    
      
  scanf("%lf%lf", &x, &y);
  distance = sqrt( pow(x,2) + pow(y,2));
  printf("%f", distance);

    return 0;
}
